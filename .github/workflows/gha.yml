name: echo pipeline
on: push
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: my-step
        run: echo "Hello ${{ github.event.issue.user.login }}, thanks for your issue!"
